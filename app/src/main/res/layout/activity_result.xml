<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#EBF8F5"
    tools:context=".ResultActivity">

    <!-- Header Section (Total Monthly Fee) -->
    <LinearLayout
        android:id="@+id/layout_result_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_header"
        android:orientation="vertical"
        android:padding="20dp"
        android:gravity="center"
        android:layout_margin="20dp">
        
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="이번 달 구독료"
            android:textColor="#555555"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_total_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="- 0원"
            android:textColor="#E53935"
            android:textSize="28sp"
            android:textStyle="bold"
            android:layout_marginTop="8dp" />

        <TextView
            android:id="@+id/tv_price_comment"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="(아무것도 구독하지 않으셨네요!)"
            android:textColor="#718096"
            android:textSize="13sp"
            android:layout_marginTop="4dp" />
    </LinearLayout>

    <!-- Scrollable Detail Analysis Section -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/bg_card"
        android:padding="16dp">

        <TextView
            android:id="@+id/tv_analysis_result"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#2D3748"
            android:textSize="15sp"
            android:gravity="center"
            android:textAlignment="center"
            android:lineSpacingExtra="8sp"
            android:text="분석 결과가 여기에 나타납니다." />
    </ScrollView>

    <!-- Stacked Buttons Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="36dp">

        <!-- Unsubscribe button -->
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_unsubscribe"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_button_cancel"
            android:textColor="#FFFFFF"
            android:text="해지하러 가기"
            android:textSize="15sp"
            android:textStyle="bold"
            android:padding="12dp"
            android:layout_marginBottom="10dp" />

        <!-- Restart button -->
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_retry"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_button_retry"
            android:textColor="#00796B"
            android:text="다시 분석하기"
            android:textSize="15sp"
            android:textStyle="bold"
            android:padding="12dp" />
    </LinearLayout>

</LinearLayout>
